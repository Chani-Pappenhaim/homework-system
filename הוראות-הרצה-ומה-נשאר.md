# הוראות הרצה + מה נשאר בפרויקט

> כל הפקודות רצות ב-**PowerShell**, מ**תיקיית הפרויקט** (השורש), אלא אם כתוב אחרת.
> אחרי כל פקודת Docker חכי שהיא תסיים לפני שאת מריצה את הבאה.

---

## חלק א' — איך מריצים את הפרויקט

### הרצה רגילה (המצב היומיומי)

```powershell
docker compose -p homework-app up -d
```

מדליק את כל 6 השירותים ברקע (`-d` = detached, לא תופס את הטרמינל).
פתחי בדפדפן: **http://localhost** (בלי לציין פורט — nginx מאזין על 80).

### בנייה מחדש (חובה אחרי שינוי קוד או תלויות)

```powershell
docker compose -p homework-app build frontend api
docker compose -p homework-app up -d
```

- `build` בונה מחדש את הדימויים של ה-frontend וה-api עם הקוד/החבילות החדשים.
- `up -d` מפעיל אותם מחדש עם הדימוי המעודכן.

> ⚠️ **חשוב עכשיו:** אחרי מעבר העיצוב ל-shadcn נוספו חבילות npm. עד שתריצי `build frontend`, האתר עדיין רץ עם הגרסה הישנה.

אם שינוי לא נטען למרות build — הוסיפי `--no-cache`:
```powershell
docker compose -p homework-app build --no-cache frontend api
docker compose -p homework-app up -d
```

### עצירה

```powershell
docker compose -p homework-app down
```

מכבה את הקונטיינרים. **הנתונים נשמרים** (הם ב-volume נפרד). כדי למחוק גם את הנתונים: `down -v` — זהירות, זה בלתי הפיך.

### לראות מה קורה (logs)

```powershell
docker compose -p homework-app ps          # מי רץ ומי בריא (healthy)
docker compose -p homework-app logs api     # לוגים של השרת
docker compose -p homework-app logs worker   # לוגים של העובדים (מיילים/AI/דדליינים)
```

---

## חלק ב' — איך ניגשים למסד הנתונים

הטבלאות **לא** על המחשב שלך ולא ב-SQL Server — הן ב-**PostgreSQL** שרץ בתוך קונטיינר Docker.
אין קובץ לפתוח; מתחברים דרך הרשת ל-`localhost:5432`.

### הדרך הנוחה — Prisma Studio (ויזואלי, בדפדפן)

```powershell
cd backend
npx prisma studio --config prisma.config.ts --url "postgresql://user:pass@localhost:5432/homework_db"
```

נפתח על **http://localhost:5555** ומראה את כל הטבלאות עם הנתונים. עצירה: `Ctrl+C`.
(השתמשי ב-`--url` ישירות — כך לא תלויים במשתנה סביבה שעלול להיות ריק.)

### דרך SQL

```powershell
docker compose -p homework-app exec postgres psql -U user -d homework_db -c "\dt"
```

⚠️ שמות הטבלאות עם אות גדולה, אז ב-SQL **חובה גרשיים כפולים**: `SELECT * FROM "User";`

### פרטי חיבור (ל-DBeaver / pgAdmin)

| שדה | ערך |
|---|---|
| Host | `localhost` |
| Port | `5432` |
| Database | `homework_db` |
| User | `user` |
| Password | `pass` |

*(אלה פרטי פיתוח מ-`docker-compose.yml`. לפני העלאה לשרת אמיתי — להחליף.)*

### 17 הטבלאות
`User`, `Group`, `StudentGroup`, `Course`, `CourseLink`, `CourseFile`, `Lesson`, `LessonProgress`, `LessonAccess`, `LessonFile`, `Assignment`, `Submission`, `Grade`, `Quiz`, `QuizAttempt`, `AiUsageLog`, `TeacherMessage`
*(+ `_prisma_migrations` — טבלה של Prisma עצמו, לא לגעת.)*

---

## חלק ג' — מה נשאר לבנות

### חובה כדי שהכל יעבוד מקצה לקצה
- [ ] **בנייה מחדש של ה-frontend** — אחרי מעבר shadcn (ראה חלק א'). זה הצעד המיידי.
- [ ] **מפתחות Cloudinary** ב-`.env` — בלעדיהם העלאת קבצים (הגשות, חומרי שיעור) לא תעבוד. צריך לפתוח חשבון ענן חינמי ב-Cloudinary ולהעתיק `CLOUDINARY_CLOUD_NAME / API_KEY / API_SECRET`.
- [ ] **הגדרת SMTP** לשליחת מיילים אמיתית — Gmail App Password (דורש אימות דו-שלבי) או Brevo/SendGrid (בלי דומיין, בלי MFA). env: `SMTP_HOST/PORT/USER/PASS` + `FROM_EMAIL` + `ADMIN_EMAIL`. בלי זה המערכת רצה, רק לא שולחת מיילים.

### פיצ'רים שהושלמו לאחרונה ✅
- [x] **ציון כפול + בדיקת AI לתלמידה** (התברר שזו מערכת אחת) — `submissionScore` (נראה מיד) + `contentScore` (מה-AI, נראה רק אחרי שהמורה מאשרת). ההסתרה נאכפת בשרת, לא בדפדפן. מוזג ל-integration/v3-complete. **דורש `docker compose build api frontend` כי ה-schema השתנה.**

### שיפורים לא-חוסמים
- [ ] הרחבת כיסוי בדיקות ב-frontend (Courses/Quiz/Reports/Home עדיין לא מכוסים).
- [ ] ייעול: בדף השיעור של המורה, מתן גישה חריגה לתלמידה סורק את כל הקבוצות אחת-אחת (הרבה קריאות רשת).

### פריסה (Deploy) — כשתרצי אתר חי
- [ ] העלאה ל-Oracle Cloud (VM חינמי + Docker Compose). דורש: החלפת סיסמאות ברירת המחדל, דומיין/HTTPS, והעברת ה-`.env` בבטחה.

---

## תזכורות מהירות (בעיות שכבר נתקלנו בהן)

- **נטפרי חוסמת SSL** → אם build נכשל עם `unable to get local issuer certificate`, זו הרשת. migrations רצות מהמחשב (לא מ-Docker) כי המחשב מכיר את התעודות.
- **שינוי לא נטען** → `build --no-cache`.
- **קונטיינר לא עולה** → `docker compose -p homework-app logs <שם השירות>` ותשלחי לי מה כתוב.
- **`.env` לא עולה ל-git** אף פעם (הוא מכיל סודות). מי שמקבלת את הפרויקט מקבלת אותו ידנית.
